[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "marketing-attribution"
version = "0.1.0"
description = "Marketing attribution ML project"

[tool.setuptools]
package-dir = {"" = "."}

[tool.setuptools.packages.find]
where = ["."]